{
  "name": "smartq-web-portal",
  "version": "1.0.0",
  "private": true,
  "description": "SmartQ Web Portal root wrapper",
  "scripts": {
    "dev": "npm --prefix admin_portal run dev",
    "build": "npm --prefix admin_portal run build",
    "preview": "npm --prefix admin_portal run preview"
  }
}
